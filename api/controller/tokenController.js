const { Token } = require('../model/index.js');

const crearToken = (req, res) => {
    try {
        const {  }

        const access_token = Token.create({

        })
    } catch (error) {
        
    }
}